{
	"fieldSchema": {
		"fields": {
			"imagePath": {
				"type": "string",
				"method": "generate",
				"description": "Return EXACTLY the file name of the input image and nothing else. Do not add explanation, path prefix, or suffix. Example: KeyFrameThumbnail_cfe02bc1-b238-4732-89db-5e5d7d4305ff.jpg"
			},
			"description": {
				"type": "string",
				"method": "generate",
				"description": "Provide a detailed natural-language description (3-5 sentences) of what is visually happening in this frame. This description is used for semantic search, so be specific and thorough. Include: the main visual content, any text visible on screen, the context or setting, and notable actions or elements. Write in the same language as the video content."
			},
			"subject": {
				"type": "string",
				"method": "classify",
				"enum": ["PERSON", "OBJECT", "SCENE", "TEXT_SCREEN", "NO_CLEAR_SUBJECT"],
				"description": "Primary visible subject of the frame."
			},
			"shot_type": {
				"type": "string",
				"method": "classify",
				"enum": ["EXTREME_CLOSEUP", "CLOSEUP", "MEDIUM", "WIDE", "UNKNOWN"],
				"description": "Camera shot type of the frame."
			},
			"emotion": {
				"type": "string",
				"method": "classify",
				"enum": ["CELEBRATING", "TENSE", "EXCITED", "CALM", "NEUTRAL", "UNKNOWN"],
				"description": "Overall emotional tone of the frame."
			},
			"objects": {
				"type": "array",
				"method": "generate",
				"items": {
					"type": "string",
					"enum": ["FACE", "TEXT_ON_SCREEN", "CROWD", "LOGO", "PRODUCT", "VEHICLE", "ANIMAL", "BUILDING", "NATURE"]
				},
				"description": "Return zero or more clearly visible items from the enum list only. If none apply, return an empty list."
			}
		},
		"definitions": {}
	},
	"_instructions": {
		"comment": "This is a sample schema. Copy this file and customize the field descriptions for your video content.",
		"usage": "python run-cu-batch.py --input-dir 'input/_KeyFrameThumbnail' --output-dir 'output/MyVideo' --schema-file 'FeldSchema_sample.json' --ensure-analyzer"
	}
}
